# Data

This directory is reserved for local project data assets and outputs.

- `raw/` holds source snapshots and intermediate exports.
- `processed/` holds dashboard-ready CSV/JSON outputs.
- `boundaries/` is where Punjab province and district boundary files should be placed.

The dashboard should read from processed outputs, not directly from Earth Engine.

