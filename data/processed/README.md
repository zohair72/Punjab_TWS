# Processed Data

This folder holds the stable, dashboard-ready outputs that the backend will serve.

Intended early artifacts:

- `tws_timeline.json`
- `dashboard_summary.json`
- `gldas_context.json`
- corresponding CSV tables for inspection and validation

